module PointGen.Address where

-- import           Data.ByteArray.Sized
-- import           Data.ByteString
-- import qualified Crypto.HDTree.Address as Crypto
--
-- data Asset = DOGE
-- data Address (a :: Asset) where
--   DogeA :: Crypto.DogeAddr -> Address 'DOGE
-- deriving instance Show (Address 'DOGE)
-- deriving instance Eq (Address 'DOGE)
-- instance Read (Address 'DOGE) where
--   readPrec = DogeA <$> readPrec
--
-- data XPub (a :: Asset ) where
--   DogeXP :: Crypto.XPub -> XPub 'DOGE
-- deriving instance Show (XPub 'DOGE)
-- deriving instance Eq (XPub 'DOGE)
-- instance Read (XPub 'DOGE) where
--   readPrec = DogeA <$> readPrec
